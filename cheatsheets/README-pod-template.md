# Pod Template

Basic Kubernetes Pod YAML template for quick creation and practice.

## Usage

- Create a simple pod using nginx image  
- Modify name, labels, and container settings as needed  

## Apply

```bash
kubectl apply -f pod.yaml
